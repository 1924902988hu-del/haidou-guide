# 海斗速查(工作名)MVP — 2026-07-19

产品决策(用户已确认):公开产品运营 / 视频只做抖音一键跳转 / 数据 op.gg + 中文站(hexdata)交叉 / 直接开工 MVP。

合规红线(公开运营):不展示海克斯强化的胜率数字(只给推荐度/梯度;英雄胜率可展示)、页脚 Riot 免责声明、站名与域名不含英雄名/Riot 商标、页面常驻数据版本与更新时间。

## 数据侦察结论(已完成)
- [x] hexdata.com.cn = 静态 JSON(/data/heroes.json、/data/heroes/{id}.json、/data/augments.json、/data/hero_formula_items.json、/data/meta.json),含中文昵称 searchTerms、强化中文描述、trios 三强化组合
- [x] op.gg 海克斯页(RSC flight):metaId/metaType 提取 强化推荐(aram-augment)、召唤师技能(spell)、出装(item,按 Starter/Boots/Core 区块切分)、技能加点(QWER 字母:前 3 = 优先级,后 15 = 升级序列);**无天赋**
- [x] op.gg 普通大乱斗页(RSC flight):内嵌完整天赋 JSON(primary/secondary style、rune ids、stat mods、pick_rate)→ 天赋来源,页面标注"参考极地大乱斗"
- [x] 静态数据:ddragon 16.14.1 zh_CN(陷阱:name=称号/title=人名)、腾讯 hero_list.js(keywords 昵称/roles 六分类,join 按 heroId)、CDragon cherry-augments zh_cn(强化图标)

## MVP 任务
- [x] pipeline/common.py — fetch(UA/重试/限速)+ RSC flight 解码器
- [x] pipeline/fetch_static.py — ddragon zh_CN ×4 + hero_list.js + cherry-augments zh_cn → 验证: 173 英雄、638 强化(170 ARAM_)
- [x] pipeline/fetch_hexdata.py — meta/heroes/augments/formula + heroes/{id}×173 → 验证: 173 个文件、含 trios
- [x] pipeline/fetch_opgg.py — 每英雄 mayhem+aram 两页解析 → 验证: 173/173 成功,0 失败 0 锚点警告(wukong slug 回退生效)
- [x] pipeline/build_site_data.py — join → index.json + heroes/{alias}.json → 验证: 173 条、"女枪→厄运小姐"命中、强化输出无胜率字段
- [x] 来源快照嵌套白名单：保留客户端、OP.GG 攻略/符文、hexdata 统计、Data Dragon 精确构建及统计日期六项证据；校验类型、真实日期、版本映射、首页/详情一致与符文标题版本
- [x] 首页索引精确白名单：仅公开卡片、搜索、筛选和视频角标实际使用的 9 个英雄字段；统计明细保留在详情，删除 7 个重复/未使用字段并锁定视频可用性结构
- [x] 首页搜索词语义闸门：移除跨英雄冲突缩写与单字母英文噪音，保留唯一常用昵称；构建、发布校验与前端输入统一 NFKC/小写规范
- [x] 首页短查询与状态反馈：单个英文字母不再触发 38–165 位模糊结果，单字中文仍可搜索；输入说明与结果变化通过 `role="status"` 暴露给辅助技术
- [x] 首页加载失败闭环：版本、计数、搜索与筛选同步进入失败状态；预置 `role="alert"` 展示安全文案及原生重新加载按钮，不暴露底层错误
- [x] site/ 前端 — index.html + hero.html + assets(移动优先、海克斯蓝金)
- [x] 图片本地化(build 产出 583 张清单 → fetch_images.py 下载,替代外链 CDN,解决加载慢)
- [x] 浏览器预览验证 + 截图交付
- [x] pipeline/update.sh — 一键全量更新(五步,后续接 cron)

## Review(2026-07-19 MVP 完成)

已验证:
- 首页:173 英雄、梯度排序、定位 tab(射手33/法师75/坦克46)、图标本地秒开
- 搜索:女枪→厄运小姐、猴子→孙悟空、蛮王/manwang→泰达米尔、ez→伊泽瑞尔置顶(修复过一次拼音中段误匹配,改为 精确>前缀>包含 三级打分)
- 英雄页(MF/蛮王抽查):四件套齐全 + 三强化组合 + 抖音跳转按钮;移动端 375px 布局正常
- 合规:强化 JSON 输出无胜率字段(自动断言)、页脚 Riot 声明、数据版本常驻顶栏
- 数据管道:op.gg 173/173 零失败;hexdata 173 文件;图片 583/583;强化描述 %i:% 模板符已清理

遗留(下一迭代):
- [x] 部署 → 已上线 GitHub Pages,见下方 2026-07-20 上线记录
- [x] cron 每日自动更新 + 更新失败告警(个人站头号死因是断更)
- [ ] 强化检索页(按强化反查适配英雄,hexdata augments.json 已有数据)
- [ ] 大乱斗平衡系数(Wiki ChampionData,调研已验证可抓)
- [x] op.gg 页面结构变更的监控(解析器锚点失效时报警而非静默)

教训:预览面板长视口截图会黑屏(compositor 问题),验证多依赖 DOM/JS 提取;op.gg RSC flight 的 metaId/metaType 是稳定解析锚点,比啃渲染树省力。

## 上线 Review(2026-07-20,用户指定 GitHub、弃 Vercel)

- [x] 手机端适配增强:iPhone 安全区 inset(顶栏/页脚)、分类标签触控高度 34px、theme-color、-webkit-text-size-adjust;375px 视口全页截图验证(首页/英雄页/页脚)
- [x] 仓库:https://github.com/1924902988hu-del/haidou-guide(public,站名 haidou 不含英雄名/Riot 商标)
- [x] GitHub Pages(Actions workflow 部署 site/ 目录):https://1924902988hu-del.github.io/haidou-guide/
- [x] 线上实测:首页图标/搜索/英雄页四件套/抖音按钮全部正常,无控制台错误

数据更新流程(当前手动):本地跑 `pipeline/update.sh` → commit + push → Actions 自动重新部署。

教训(重要):本机网络推 git 大包必挂(>2MB pack 即 "remote end hung up",重试无用;http.postBuffer/HTTP1.1 均无效),但 GitHub API 小请求稳定——图片是用 Git Data API 逐 blob 上传后建树提交的(583/583 成功)。以后含大量图片的更新:小批量提交(<1MB/批)或复用 API 上传方案。

待办(上线后):
- [x] 数据自动更新(GitHub Actions 每日更新、验证、提交、部署;失败 Issue 告警)
- [x] 强化描述里的 "?" 占位符(Wiki 数值模板未填充,如"获得?法术强度")修复
- [ ] 国内访问速度评估;必要时自定义域名(不得含英雄名/Riot 商标,备案问题届时评估)

## 续接 Review(2026-07-29,Codex)

- [x] 数据升级:Data Dragon 16.15.1(对外静态版本 26.15),hexdata 最新统计仍为 26.14 / 2026-07-24;顶栏分开显示,不伪装成同版本统计
- [x] 26.15 特殊模式兼容:Data Dragon 新增的 60 个 `Jade_` 变体不会被当成真实英雄或下载无用图片;正式英雄仍为 173
- [x] op.gg 稳定性:按补丁断点更新;HTTP 截断响应需通过业务字段完整性验证;173/173 均有强化、技能、出装和符文,失败时非零退出
- [x] 发布前验证:`validate_site_data.py` 检查索引/详情一致、图片引用、版本、视频目录、强化胜率红线和描述占位符
- [x] 双端页面:桌面详情双栏、手机单栏和 2×2 统计卡;390×844 与 1440×900 均无横向溢出、无控制台错误
- [x] 视频资料层:建立 `data/videos/catalog.json` 与核对状态;已对机器人公开视频实际抽帧核字幕,只保留来源与结构化摘要,不托管原视频
- [x] 自动更新:`update-data.yml` 每日 12:30(北京时间)运行;复用 raw cache,有变更才提交,随后部署 Pages;失败开 Issue,恢复自动关闭

下一步不是上线阻塞项:
- [x] 抖音视频情报管道:TikHub 搜索候选 → BiliNote 多模态理解 → 英雄/强化/出装/符文/技能/证据结构化 → 质量闸门 → 发布与全站重建验证
- [x] 博主实战 UI:首页视频数量标记;详情页展示来源、发布日期、证据类型、版本风险、结构化搭配和可展开时间戳证据
- [x] 置信度边界:模型自评百分比只用于内部候选闸门;公开构建剥离字段,页面不把未校准分数显示成正确率
- [x] 公开 JSON 最小化:内部置信度/复核对象以及未展示的抓取时间、审核时间、分析状态、时长和互动数只留在审计目录,173 份英雄详情按白名单输出读者需要的来源、日期、搭配、证据与版本状态;5 位视频英雄 / 168 位 OP.GG 回退计数不变
- [x] 搜索框键盘焦点:修复输入框 `outline:none` 覆盖全局焦点环的问题,用高对比 `:focus-within` 外框同时覆盖桌面与手机;Tab 可见、无横向溢出、控制台 0 错误
- [x] 定位筛选键盘焦点:按钮只初始化一次,切换时原地更新 `active` 与 `aria-pressed`;Space 激活后焦点留在按钮,桌面/手机结果一致
- [x] 定位筛选组语义:用具名 `role="group"` 关联 7 个原生切换按钮;可访问性树、键盘状态及桌面/手机布局均已验证
- [x] 英雄别名 DOM 注入防护:URL 参数使用正向白名单并编码路径段,错误改用 `textContent`;恶意参数无法创建元素或请求资源,173 个合法别名均兼容
- [x] 图片引用发布闸门:560 个唯一图标均限定为仓库内 `assets/...`;外部 URL、属性逃逸、路径穿越、查询/片段在构建时失败关闭
- [x] 真实样例验收:机器人视频已核对 4 个时间点与六件装备;桌面 1440×900、手机 390×844 无横向溢出,控制台 0 错误
- [x] 配置 `TIKHUB_TOKEN` 与带多模态模型的本地 BiliNote;2026-07-29 已搜索 173 位英雄,276 次 TikHub 搜索约 $2.76,发现 84 条近 45 天候选;视频的目标英雄、每件出装、每项强化与符文都必须在时间戳证据中逐名出现,且装备必须适用于嚎哭深渊或有海克斯大乱斗模式样本;证据时间戳必须严格早于 `durationSeconds`,网页只发布搭配名称与确定性摘要,不展示无证据理由/打法;梅尔记录因证据只证明强化与装备、未证明英雄身份而回退 OP.GG;Token 曾在对话中暴露,仍需在 TikHub 控制台轮换
- [x] 证据子集发布:离线重判 84 条缓存草稿,只公开当前词典合法且有逐项时间戳证据的搭配,原始模型分析和省略原因留在内部草稿;正式覆盖由 3 位提升到 7 位(Aatrox/Yone/Lillia/Rammus 新增),其余 166 位继续保留 OP.GG,本轮没有新增 TikHub/BiliNote 请求或费用
- [x] 批次费用口径:恢复断点分别记录本轮新增搜索、断点累计、新分析与缓存重判;请求上限只约束本轮,价格仅按 TikHub 公开区间计算本轮搜索上限且明确不代替账单,避免离线重判重复报告历史 276 次搜索
- [x] 视频发布日期衰减:发现与网页共用 45 天近期窗口,正式记录写入 `expiresAt`;到期后目录保留审计记录,英雄页自动隐藏视频并回退原有 OP.GG,不凭年龄单独伪称机制已过时
- [x] 视频搭配失效回退:每次静态数据更新后重查视频内强化、装备、符文与召唤师技能;已删除或不再适用于海克斯大乱斗的记录保留在目录供审计,但构建自动隐藏并回退 OP.GG
- [x] 站点快照补丁直接影响:用官方 26.15 公告标注补丁生效前的 Kindred 伤害修正与 Soraka 治疗修正变化,视频卡保留搭配证据但明确不能证明调整后的强度,并提供官方可点击来源
- [x] 证据状态不夸大:保留 `multimodal-reviewed` 表示关键帧+转写文本的处理流程,但网页徽标只按正式记录实际公开的 `frame` / `subtitle` / `audio` 证据类型生成;验证器拦截标签与证据不一致
- [x] 静态快照语义:顶栏和详情页分别显示客户端资料、OP.GG 攻略、hexdata 统计版本,统计日期在手机端不再隐藏并按本地日历显示距今天数;OP.GG 缓存按页面自身资源版本失效,不再把 Data Dragon 升版误当作 OP.GG 已同步
- [x] hexdata 批次一致性:全量文件先暂存,下载首尾复核 buildId,正式缓存用 SHA-256 清单绑定 173 份英雄明细;合成前失败关闭,避免中断后混合新旧构建
- [x] 扩充并收窄“已看画面”视频覆盖率;当前 5 条正式记录均保留发布日期、核对日期、版本风险、目标英雄身份和时间戳证据;锤石只公开画面与原始语音都能闭环的“蛋白粉奶昔”,不再把 ASR 写作“碟角龙”的结果强行发布为“叠角龙”;其余 168 位继续回退 OP.GG,不得把 `metadata-only` 当攻略
- [x] 分析阶段直接保存 BiliNote 截图与视频时间戳映射:80/84 条缓存草稿成功绑定并复制 551 帧,逐帧保存 SHA-256;4 条没有合法截图标记,不可伪造映射;公开 `frame` 证据必须命中同时间戳清单
- [x] 用已持久化帧逐像素复核 7 条正式记录的 10 条公开画面结论;亚托克斯“狂妄”和索拉卡“灵魄炸弹”无法由像素唯一证明,已回退 OP.GG;千珏纠正为“魄罗蛮冲”,永恩移除无画面支持的“升级:无尽之刃”;当前 5 条正式记录均绑定逐帧审查表与截图 SHA-256
- [x] 复核 5 条正式记录仍依赖的 5 条公开“字幕”结论:确认 BiliNote Markdown 是生成笔记、`*_transcript.json` 才是原始 ASR;锤石撤下“叠角龙”,蛋白粉奶昔改标语音、目标英雄改绑 00:21 画面;永恩改标原始语音;拉莫斯出装从错误的 00:21 改绑 00:57 画面;新增原始转写/截图哈希漂移失败关闭
- [ ] TikHub Token 轮换并确认后,对 168 位 OP.GG 回退英雄按近期性与数据价值分批恢复发现;每批先给出最大调用数与公开价格上限,继续复用 276 次历史搜索断点
- [ ] 强化检索页(按强化反查适配英雄)
- [ ] 大乱斗平衡系数
- [x] 抖音搜索链接边界：手动搜索 CTA 复用 HTTPS 域名白名单，发布校验限定 `www.douyin.com/search/`；173 个现有链接通过
- [x] 首页数据到 DOM 边界：英雄卡片字段按上下文转义、别名 URL 编码且发布时白名单校验，加载错误改用 `textContent`
- [x] 详情页基础攻略 DOM 边界：非视频英雄、强化、装备、符文、技能与召唤师字段统一按 HTML 正文或属性上下文转义
- [x] 英雄统计发布契约：首页与详情统一校验段位、概率、样本场次和 KDA 的类型/有限性/业务范围，并拒绝两处数值漂移
- [x] 固定攻略枚举发布契约：英雄定位、强化稀有度与技能优先级/15 级序列必须完整、互不冲突且命中允许列表
- [x] 出装结构发布契约：过滤 OP.GG 重复/畸形三件套并从后续有效组合补足；装备 ID、名称、图片、数组形状与组内唯一性失败关闭
- [x] 符文页结构发布契约：173 位英雄 / 346 套现有 OP.GG 符文页均为 4 主系 + 2 副系 + 3 碎片；来源、主副系、名称、图片、采用率与页内唯一性失败关闭
- [x] 召唤师技能结构发布契约：173 位英雄均有主流/备选 2 组，每组 2 项；ARAM/KIWI 名称、名称—图标映射、组内唯一性与两组差异失败关闭
- [x] 强化推荐与三强化组合发布契约：173 位英雄的强化 ID、名称、描述、稀有度、图片、推荐标记及 1027 组恰好 3 项的组合结构失败关闭；移除由 `winRateTier` 改名公开的 `T1/T2` 档位，只保留推荐排序与样本场次
- [x] 推荐排序信号最小化：构建时继续用 hexdata 的 `hexScore` 排序，但从 3,460 条公开强化/装备记录移除未渲染的数值分数；强化、组合及装备对象使用字段白名单，页面仅保留可见推荐标签与样本量
- [x] 英雄详情顶层白名单：对照前端真实读取点，从 173 份公开 JSON 移除重复身份 `id/alias`、未加载外部 `splash` 与未展示 `sources`；身份一致性改用文件别名、名称及本地图标校验
- [x] 英雄详情加载失败闭环：区分无效英雄地址与数据请求失败，固定安全文案和版本状态，使用预置警报区、原生重载按钮及返回列表双恢复路径
- [x] 公共 JSON 加载超时：同源首页/英雄数据的请求与正文解析共用 20 秒上限，使用广泛兼容的 `AbortController` 终止悬挂连接并清理计时器，不自动重试
- [x] 公共 JSON 浏览器缓存：从每次强制条件请求的 `no-cache` 改为遵循源站新鲜期的 `default`；GitHub Pages 当前 HTML/JS/JSON 均返回 `max-age=600`，10 分钟内重复导航可直接复用缓存；不使用可能无限陈旧的 `force-cache`，也不宣称离线可用
- [x] 图标失败降级与替代文本：与邻近同名文字重复的英雄、强化、装备、符文及召唤师图标统一使用空 `alt`，并移除仍会生成重复可访问名称的 `title`；只有无邻近名称的 OP.GG 核心三件套图标保留名称，固定尺寸在资源失败时维持布局
- [x] PNG 可解码性发布闸门：仅用 Python 标准库检查站点图片签名、关键块顺序、CRC、非零尺寸、色彩/位深、完整 IDAT zlib 数据、解码长度与扫描线过滤器；589 个现有 PNG 均通过，并由无第三方包模式与 Pillow 独立解码交叉验证
- [x] 图片清单与部署资产同步：同补丁回退保留的 OP.GG 核心出装重新登记 Data Dragon 图片；下载、存在性复核全部成功后才按完整清单清理 9 张零引用历史 PNG，任何失败均在清理前退出
- [x] 图片下载自愈与原子替换：现有 PNG 每轮先做完整解码，损坏文件与缺图一起重抓；下载写入同目录安全临时文件，经标准库 PNG 闸门、`flush/fsync` 后才用 `os.replace` 替换，失败保留旧文件并清理临时产物
- [x] 图片来源与快照绑定：英雄/装备/技能精确匹配当前 Data Dragon 构建及本地类别/文件名，符文限定 Riot perk-images；强化仅允许 CommunityDragon 的 global/default 现行 Kiwi/Cherry 路径，拒绝第三方主机、版本漂移、类型错配及 URL 参数
